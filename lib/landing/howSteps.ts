/** Landing copy, kept in reading order for both desktop and mobile. */
export const HOW_STEPS = [
  {
    number: "01",
    title: "Resolve",
    body: "We find every version of that stock that exists onchain, across every provider we support.",
  },
] as const;
